# testing.md — テスト・品質チェック

## テストフレームワーク

現時点ではテストフレームワーク未導入。今後整備予定。

## テストファイルの配置

未確認 — テストフレームワーク導入時に決定する。

## テストの実行方法

```bash
# テストフレームワーク未導入のため、現時点ではビルド成功をもって検証とする
npm run build
```

## Lint / フォーマット

現時点では lint・フォーマッタ未導入。今後整備予定。

## 現在の品質チェック手段

1. `npm run build` が正常に完了すること
2. `npm run dev` でローカルサーバーを起動し、ブラウザで動作確認
3. GitHub Actions（`research-and-deploy.yml`）が正常に完了すること

## テストを書く際の注意点

- テストフレームワーク導入後、実装変更時は対応するテストも必ず追加・修正する
- バッチスクリプト（`scripts/`）は外部 API に依存するため、モック戦略を検討する
