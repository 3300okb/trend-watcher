# testing.md — テスト・品質チェック

## テストフレームワーク
- 未導入（`package.json` に `test` スクリプトなし）

## テストファイルの配置
- 現在は未配置（`__tests__/` / `tests/` / `*.spec.*` / `*.test.*` なし）

## テストの実行方法
```bash
# 現在は自動テスト未導入のため、代替検証を実施
npm run build
```

## Lint / フォーマット
```bash
# lint / format スクリプトは未導入
# 代替として最低限ビルドを実行
npm run build
```

## 未導入項目の扱い
- lint やテストが未導入の場合は「未導入」と明記し、代替の検証コマンド（例: ビルド、型チェック）を記載する

## テストを書く際の注意点
- 実装変更時は対応するテストも必ず追加・修正する
- 外部 API（RSS/翻訳/Supabase）依存部分はモック化し、ネットワーク非依存で実行できる形にする
- バッチ処理の副作用（ファイル書き込み）は一時ディレクトリで検証する
