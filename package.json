{
  "name": "trend-watcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:config": "node scripts/sync-runtime-config.mjs",
    "job:fetch": "npm run sync:config && node scripts/fetch-trends.mjs",
    "build": "npm run sync:config && node scripts/build-static.mjs",
    "dev": "python3 -m http.server 8080 --directory public"
  }
}
